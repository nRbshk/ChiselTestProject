package apb

import chisel3._
import chisel3.util._





class APB_master(AW: Int = 8, DW: Int = 32, NSLAVES: Int = 1) extends RawModule {
    // val IO = IO(new Bundle {
    //     val in = Input(for (slave <- NSLAVES - 1) {
    //         new APB_interface_slave(DW)))
    //     }
    //     val out = Output(for (slave <- NSLAVES - 1) {
    //         new APB_interface_master(AW, DW))
    //     }

    // })

    



}